const { getDevice } = require('./device');
const { getElemHandle, getElemHandles } = require('./getElemHandle');
const { getLocaleHeaders } = require('./locale');
const { getLocation } = require('./location');
const { getUrl } = require('./url');

module.exports = {
  getDevice,
  getElemHandle,
  getElemHandles,
  getLocaleHeaders,
  getLocation,
  getUrl,
};
